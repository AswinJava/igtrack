{
  "name": "@igtrack/monitoring",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "IGTrack database-backed job runner: claim/execute/complete, profile + follower scans, checkpoints, retries",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "tsx src/main.ts",
    "run-once": "tsx src/run-once.ts"
  },
  "dependencies": {
    "@igtrack/core": "workspace:*",
    "@igtrack/database": "workspace:*",
    "@igtrack/ingestion": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3"
  }
}
