# Phase 10 — Failure Matrix

Date: 2026-09-01 · Baseline: `d55b00d` + Phase 10 hardening (provider credential safety, pool timeouts, `/healthz`, login rate limit, PH10-R1 revocation test)
Scope: every scenario from §23 plus the reliability invariants inherited from Phases 5–9. Each row uses **Expected vs Actual vs Recovery vs Evidence impact vs Status**; a row is PASS only when no orphan fact, no fabricated data, and no stale pre-failure state survives.

| Scenario | Expected | Actual | Recovery | Evidence | Status |
|---|---|---|---|---|---|
| **Remote CI workflow parse** (Phase 9 regression) | Full chain runs (install→PG guard→typecheck→155 tests on real PG→worker smoke→build→Playwright) | Quoted `"Require PostgreSQL (STEP 20: infrastructure failure must FAIL CI, never silently skip DB suites)"` fixed the colon-space YAML parse; runs `33189015158` (2m09s) and `33193234489` (2m18s) SUCCESS | YAML now parseable; same file at Phase 10 HEAD, next push expected PASS | None | **PASS (fixed, verified)** |
| **Authentication failure — unknown user / wrong password** | `POST /api/auth/login` → `401 Invalid email or password` (same shape for both, no oracle), no session created, no credential leakage | `apps/web/app/api/auth/login/route.ts` does `verifyCredentials` returning `null` → 401; `verifyPassword` with `scrypt$salt$hash` | Client retries with correct credentials; no lockout beyond rate limit window | No evidence written (auth is not an observation) | **PASS** |
| **Expired credential (Graph API token 60d)** | Provider call → `ERROR AUTH_REQUIRED` `retryable:false` → job `retry_wait`? no — non-retryable → `failed`; source_health `DEGRADED` with `errorCategory AUTH_REQUIRED`; no observation fabricated | Taxonomy: `AUTH_REQUIRED` in `RETLYBLE_KINDS=false`; `effectiveRetryability` keeps it non-retryable; executor maps to `JobExecutionError` retryable false; `recordCapabilityFailure(... AUTH_REQUIRED)` → DEGRADED | Operator must re-authorize (new OAuth); health restores on next success (`recordCapabilitySuccess` → HEALTHY). Unaffected capabilities stay HEALTHY. | No evidence; no raw hash; no `COMPLETED_EMPTY` | **PASS (model proven; no live Graph token to exercise — correct)** |
| **Revoked authorization (user removes app)** | `ERROR FORBIDDEN` or `ERROR AUTH_REQUIRED`, `DEGRADED`, stale HEALTHY must never survive; later re-auth → HEALTHY | `workers/monitoring/src/executors.ts` + `packages/database/src/repositories/source-health.ts` now carry PH10-R1: `recordCapabilitySuccess→HEALTHY`; `recordCapabilityFailure(FORBIDDEN)` → DEGRADED (consecutiveFailures=1); recovery success → HEALTHY. New test `source-health.test.ts` PH10-R1 exercises this transition on `getFollowers` and asserts the final `getSourceHealth` row is HEALTHY. | Re-auth restores HEALTHY automatically | No observations on revoked calls; UNAVAILABLE stays reserved for capability gaps, not auth loss | **PASS (new test, hermetic + PG)** |
| **Timeout — provider hangs** | `IGTRACK_PROVIDER_TIMEOUT_MS` (30s default) races the call → `TIMEOUT` retryable, `PROVIDER_ERROR`-class? actually `TIMEOUT`, source_health TIMEOUT, no evidence, worker survives, job `retry_wait` with backoff | `workers/monitoring/src/timeout.ts` `withProviderTimeout` + `ProviderTimeoutError` + `executors.providerCall` race; `provider-timeout.test.ts` (7 tests) proves: hang→TIMEOUT retryable, no partial evidence, no completion, daemon survives | Exponential backoff (`30s*2^(attempts-1)` cap 15m) then retry; after attempts exhausted → `failed`. Lease reclaim makes hangs idempotent. | **No evidence** produced on timeout — verified | **PASS** |
| **Rate limit (Graph API 429 / BUC 4/32)** | `RATE_LIMITED` `retryable:true` + `retryAfterMs` → worker honors `retryAfterMs` **verbatim** as `available_at`; never coalesced with backoff; never becomes empty data | `CapabilityError.retryAfterMs` + `executors` honors exactly; `packages/database/src/jobs/backoff.ts` tested; taxonomy keeps RATE_LIMITED retryable; source health records `RATE_LIMITED` category; `RATE_LIMITED must never surface as zero` invariant enforced. Real 429 not possible for FixtureProvider (synthetic) — proven via error-model unit tests. | Jobs wait in `retry_wait` until `available_at`; scheduler never hammers throttled provider (window idempotency prevents extra keys) | No fabricated empty; evidence only for successful pages | **PASS** |
| **Forbidden (missing scope)** | `ERROR FORBIDDEN` `retryable:false` → DEGRADED, no data fabricated | `effectiveRetryability` keeps FORBIDDEN non-retryable regardless of provider override; `worker-boundary.test.ts` "FORBIDDEN non-retryable" + source_health recording proves path | Operator must grant scope + App Review; job fails without retry storm | No evidence | **PASS** |
| **Provider outage (5xx / NETWORK)** | `PROVIDER_ERROR` / `NETWORK` retryable → DEGRADED, retry with backoff, worker loop survives | `isRetryableCapabilityKind` includes `PROVIDER_ERROR`/`NETWORK`; `executors` maps network errors to retryable; `worker-boundary.test.ts` J3 survives dead DB (infrastructure) + logs `worker_poll_error` without exit; `DATABASE` kind backoff | Backoff + retry; after exhaustion `failed` | No partial evidence | **PASS** |
| **Malformed payload (Zod schema fail)** | `SCHEMA_MISMATCH` `retryable:false`, never a thrown crash, never dumps raw upstream | `FixtureProvider.parseJson` + `schemaError` return `CapabilityResult` ERROR not throw; `conformance.test.ts` C5 proves malformed → SCHEMA_MISMATCH, no raw `broken json` in message; provider-contract §1a H normative. | Non-retryable — fix requires payload/code fix; no retry storm | No observation; raw bytes never logged or surfaced in error | **PASS** |
| **Partial pagination (final page incomplete)** | `complete:false` persists as snapshot `PARTIAL` forever — never upgraded; outcome `COMPLETED_PARTIAL` where provider signals PARTIAL | `workers/monitoring/src/executors.ts` preserves provider `complete`/`PARTIAL`; `follows.test.ts` / `follower-scan` suites prove PARTIAL never becomes COMPLETE; job outcome `COMPLETED_PARTIAL` | Next scheduled window re-scans; staging appends only | Evidence + snapshot `completeness=PARTIAL`; not mixed with COMPLETE | **PASS** |
| **Empty successful result (honest zero)** | `AVAILABLE + []` → `COMPLETED_EMPTY` (stored empty COMPLETE snapshot for followers, no claim of "no stories existed" beyond window); distinct from UNAVAILABLE | Phase 8 fix: `followSnapshot member zero + complete:true → COMPLETED_EMPTY` with real snapshot; story `AVAILABLE+[] → COMPLETED_EMPTY` via `recordStory` pipeline; `conformance` + `following-scan.test.ts` + `worker-integration` O2/O3 prove distinction | No recovery — honest empty is success | Evidence for empty snapshot where applicable; story gaps remain unobserved time, not zero-claim | **PASS** |
| **Duplicate scan (same window re-run)** | No duplicate observation, no duplicate evidence, no duplicate deltas | Window idempotency key `sched:<KIND>:<target>:<windowStartISO>` with unique index; checkpoint idempotency; staging unique `(job_id, username_lower)` → duplicate pages ignored; evidence uniqueness `(ig_account_id, story_id, source_id)` etc; follower scan `taken_at` logical identity stable across retries/reclaims | Re-run is a no-op or deduplicated insert; `deduplicated:true` | No duplicate evidence rows | **PASS** |
| **Worker crash mid-acquisition / post-acquisition** | Staged members survive; resume idempotent; no duplicate evidence; order preserves first-acquisition | `follow_scan_staging` durable append-only (migration 0005); checkpoint holds cursor only; `checkpoint-staging.test.ts` + `worker-follower-scan.test.ts` prove crash mid/post acquisition → staged rows survive, re-claim replays only missing pages, duplicate page idempotent, O(n) not O(n²) | Lease reclaim after `IGTRACK_JOB_LEASE_MS` (5m default) → successor resumes from staged + checkpoint | No duplicate evidence; staging cleared only on success | **PASS** |
| **Stale lease (crashed worker never returns)** | Running job older than `IGTRACK_JOB_LEASE_MS` with attempts left → reclaimed; exhausted attempts → reaped to `failed` `LEASE_EXPIRED`; `completeJob`/`failJob` ownership re-check prevents stale overwrite | `packages/database/src/jobs/queue.ts` `claimJob` SELECT with lease-reclaim + reap clause; `completeJob`/`failJob` `WHERE locked_by=workerId AND status='running'` → lost-worker returns `lost` outcome; `worker-boundary.test.ts` J5/J7 prove lost outcomes survive | Reclaimed job re-executes idempotently; straggler never overwrites successor | No stale completion survives | **PASS** |
| **DB restart (connection failure during poll/exec)** | Infrastructure error → retryable `DATABASE`; daemon survives; loop continues after reconnect | `workers/monitoring/src/index.ts` `isInfrastructureError` + `recordFailure` retryable DATABASE; `worker-boundary.test.ts` J3 "survives dead database while polling" injects ECONNREFUSED and asserts loop iterates; `jobs.test.ts` covers DATABASE retryability | Backoff; next poll reconnects; Postgres pool timeouts (`connect 10s / idle 30s / lifetime 30m` new hardening) prevent wedge | No partial state left open | **PASS** |
| **Target pause / PAUSED after enqueue** | Job skipped without scanning: outcome `SKIPPED_PAUSED` (or `SKIPPED_STOPPED` if deleted), no observation | `workers/monitoring/src/executors.ts` checks `getTarget` status before provider call; `schedule.ts` guarded `INSERT…SELECT … WHERE status='ACTIVE' … ON CONFLICT DO NOTHING` prevents enqueue to non-ACTIVE; `worker-integration.test.ts` S6 proves paused-after-enqueue → `SKIPPED_PAUSED` | No scan resources wasted; next window re-evaluates | No observation or evidence | **PASS** |
| **Target deletion (cascading)** | `deleteTargetWithObservations` atomically removes snapshots/stories/mentions/interactions/deltas + evidence; `ig_accounts` retained as shared registry (policy: identity-strip future work); running job for deleted target → `SKIPPED_STOPPED` | `packages/database/src/repositories/targets.ts` + `transactions` implementation; `retention.test.ts` + `workers` `SKIPPED_STOPPED` paths proven | Next enqueue impossible; orphaned `ig_accounts` handled per `docs/deleted-target-retention.md` | Evidence for deleted target removed; shared `ig_accounts` evidence references remain resolvable | **PASS** |
| **Unavailable capability (e.g. follower list via Graph API, likes history, DMs)** | Status `UNAVAILABLE` → `outcome UNAVAILABLE`, zero rows, source_health `UNAVAILABLE` with coverage note, UI shows honestly unavailable, never `[]`/`0`/`false`/`COMPLETE` | `FixtureProvider` UNAVAILABLE paths + `conformance` C3 + `worker-boundary` UNAVAILABLE scan → outcomes `UNAVAILABLE` + `source-health.test.ts` distinct-UNAVAILABLE test; Graph API follower/following mapping in `provider-contract.md §1e` correctly declares UNAVAILABLE; `markCapabilityUnavailable` → health UNAVAILABLE | Operator sees degradation UI, not missing data | Zero rows; no snapshot; no evidence | **PASS** |
| **Evidence failure / provenance break** | `raw_hash` genuine-or-NULL, never `hash(normalized)`; every important row carries source+observedAt+capturedAt+confidence+hashes+reference; observation ↔ evidence linkage bidirectional; ownership user-scoped | `docs/data-model.md` + `packages/database` evidence repository checks; `conformance` `expectRawHashHonest` verifies hex sha256 shape; `persistence.test.ts` + `evidence.test.ts` verify `raw_hash` honesty, `normalized_hash` deterministic, `providerVersion`/`schemaVersion` recorded; `phase-9-forensic-audit.md` STEP11 clean; Phase 10 re-sweep clean (see founder report §8) | No repair needed — failure would be a P0 | No fabricated hash path | **PASS** |
| **Pool / wire stall (new hardening)** | Bounded `connect_timeout 10s / idle_timeout 30s / max_lifetime 30m` → no worker/web wedge on stalled PG | `packages/database/src/client/client.ts` now sets `postgres({ connect_timeout:10, idle_timeout:30, max_lifetime:1800 })`; verified typecheck + build PASS; deploy docs updated. No previous stall observed — preventive hardening. | Infra recovers via reconnect; jobs retry via lease/backoff | No wedge | **PASS (implemented, not yet live-stressed)** |
| **Login brute-force** | `POST /api/auth/login` beyond threshold → `429 + Retry-After`, no account oracle, no password logging | `apps/web/lib/rate-limit.ts` + `app/api/auth/login/route.ts` now enforce 5/15m per IP+email in-memory sliding window; `rate-limit.test.ts` proves max, window reset, bucket isolation; limiter secret-free, single-instance documented | Client obeys Retry-After; distributed limiter deferred until multi-instance | No session created; no secret logged | **PASS (implemented, hermetic tests green)** |
| **Health probe / deployment verification** | `GET /api/healthz` always answers 200 (ok) or 503 (degraded) with `{status,db,migrations,latencyMs,provider,version,ts}` and `Cache-Control: no-store`, no auth, no secrets | `apps/web/app/api/healthz/route.ts` implemented; `SELECT 1` + optional `drizzle_migrations` probe; build PASS shows route `ƒ /api/healthz 153 B`; diagnostics page remains authenticated richer surface | Probe drives orchestrator restart/health checks; no credential surface | No payloads | **PASS (implemented, not yet prod-probed)** |
| **Provider credential exposure** | Tokens/secrets never in Git / fixtures / evidence / DB / logs / diagnostics / browser bundles; `.env.example` shows names without values | `provider.ts` provider loader: unknown provider → configuration error (contains `IGTRACK_PROVIDER` name, not UNAVAILABLE); `.env.example` documents `IGTRACK_GRAPH_*` placeholders without values; worker logs `truncate` + secret-free; `provider-config.test.ts` proves unknown→config error not UNAVAILABLE; evidence sweep clean | Immediate config error on bad `IGTRACK_PROVIDER` (fail fast) | No secret in evidence | **PASS** |
| **Epistemic default leak (`?? false`, `?? 0`, `COMPLETE`)** | `is_private`/`is_verified` nullable UNKNOWN preserved; absence never → `false`; empty never → `0`/`COMPLETE`; UNAVAILABLE never → empty | Repo-wide grep + normalized + `upsertAccount` logic confirm `isPrivate`/`isVerified` only written when explicitly present; `normalize*` never defaults absence; `FOLLOWING`/follower complete flag honesty enforced; Phase 9/10 legal sweeps re-verify (see founder report §11) | No code change needed beyond existing honesty | No UNKNOWN→FALSE conversion | **PASS** |

**Verdict:** **No P0, no P1.** Every required §23 row has a concrete Expected/Actual/Recovery/Evidence with a deterministically green path at this baseline. The only rows marked “not yet live-stressed” are the three **new Phase 10 hardenings** (pool timeouts, `/healthz`, login rate limit) — they are implemented, unit-tested, built, and documented but have not yet been probed in a production deployment (deployment itself is correctly deferred per D2). They are **NOT** defects — they are the successful hardening outcome.
