{
  "name": "@igtrack/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "IGTrack domain types, provider contracts, and diff primitives (zero runtime deps)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root ../.."
  }
}
